node_modules/
dist/
coverage/
*.log
.DS_Store

# Environment files — only .env.example is committed (PRD §12.3)
.env
.env.*
!.env.example

# Compact compiler output. Whether keys/zkir get committed is a Day-2 decision
# after the first real compile (PRD §13 note; docs/DECISIONS.md D-002).
contracts/managed/

# Yarn 4
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Local devnet deployments are regenerated by `tacitpay demo seed` against
# every fresh chain, so they are machine-specific. Only real-network
# deployments (preview, mainnet) are committed, per PRD §13.
deployments/local.json
deployments/undeployed*.json

# Tooling scratch
.vite/
.firecrawl/
