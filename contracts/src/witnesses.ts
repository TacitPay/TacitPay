// Witness implementations (PRD §6.3) are supplied to the generated contract
// module from TypeScript and return [newPrivateState, value] tuples.
// The real merchantSecret / payerSecret witnesses land together with the
// Wave 1 contract (Day 1–2); the placeholder contract declares no witnesses.
export const witnesses = {};
