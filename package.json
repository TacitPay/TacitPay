{
  "name": "tacitpay",
  "private": true,
  "version": "0.1.0",
  "description": "Private invoicing & settlement on Midnight — commitments on-chain, amounts and parties private, provable on demand.",
  "license": "Apache-2.0",
  "packageManager": "yarn@4.18.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "contracts",
    "packages/*"
  ],
  "scripts": {
    "compile": "compact compile contracts/tacitpay.compact contracts/managed/tacitpay",
    "build": "yarn workspaces foreach -A --topological run build",
    "test": "yarn workspaces foreach -A run test",
    "test:int": "yarn workspaces foreach -A run test:int",
    "env:up": "echo 'TODO(PRD §12.5): clone midnightntwrk/midnight-local-dev and wire docker compose up here' && exit 1",
    "env:down": "echo 'TODO(PRD §12.5): wire docker compose down here' && exit 1",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "yarn workspaces foreach -A run typecheck"
  },
  "devDependencies": {
    "eslint": "10.9.0",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0"
  }
}
